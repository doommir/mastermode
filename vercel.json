{
  "framework": null,
  "buildCommand": "npm install && npm run build",
  "devCommand": null,
  "outputDirectory": "build",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/generate-flashcards.js" }
  ]
}
